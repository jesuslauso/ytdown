# 純前端下載工具 AI 開發指令模板 (極簡版)

**使用說明：**
直接複製以下【】內的完整文字，並在最後面填寫你的三項需求，然後發送給 AI 即可。

---

【**請複製以下內容發送給 AI**】

你好，我需要你幫我開發一個純前端、無後端依賴的下載工具。這個工具必須是一個單一的 HTML 檔案。請嚴格遵循以下的架構限制與設計原則來撰寫程式碼，不需要與我確認，直接輸出完整的 `index.html` 程式碼即可。

### 架構限制與設計原則 (必須嚴格遵守)

這個專案有嚴格的環境與相容性限制。所有的 HTML、CSS 與 JavaScript 必須寫在同一個 `index.html` 檔案中，絕對不可使用任何構建工具或 npm 套件。

為了確保在舊版 iOS 和舊版 Chrome 上能正常運行，**絕對禁止**使用 `let`、`const`、箭頭函數、`async/await`、模板字串或展開運算子。請全面使用 `var`、傳統的 `function` 宣告以及基於 `.then()/.catch()` 的 Promise 鏈。

這個檔案最終會部署在 GitHub Pages 等 HTTPS 環境上。因此，所有呼叫的第三方 API 必須支援跨來源請求（CORS）。此外，為了防範 XSS 攻擊，在將任何從 API 獲取的字串或使用者輸入插入 DOM 之前，必須實作並使用一個 `escHtml()` 函數進行字元跳脫。

### 核心邏輯要求

在資料獲取與錯誤處理方面，請使用 Piped API (或支援 CORS 的替代 API) 作為資料來源。你必須實作靜默重試機制。如果主 API 解析失敗（例如遇到限流），程式應在背景自動切換到備用 API 陣列進行重試。在這個過程中，不要讓使用者介面崩潰，也不要輕易重置全域的 API 狀態變數。

在處理檔案大小與下載邏輯時，如果 API 提供的檔案大小不準確，請在解析完成後，透過發送 `HEAD` 請求到下載網址來獲取真實的 `Content-Length`。請特別注意非同步更新的問題：`HEAD` 請求完成後，必須重新呼叫渲染 UI 的函數，確保畫面顯示正確的大小。

下載方式請使用 Fetch API 獲取 Blob，再透過 `URL.createObjectURL` 觸發瀏覽器下載。盡量提供原始格式直接下載，避免在瀏覽器端進行無謂的重新編碼。

若支援多網址批量下載，請實作佇列管理，解析請求可以適度並發，但「下載 Blob」的過程必須**嚴格序列化（一次下載一個）**，防止行動裝置記憶體耗盡。若需音訊剪輯，請使用 Web Audio API (`OfflineAudioContext`) 進行解碼與裁切。

### 本次開發需求

請根據以下三項需求，設計並實作這個下載工具，並在程式碼中加入適當的繁體中文註解說明核心邏輯：

1. **主要用途**：`<請填寫你要下載什麼，例如：下載 YouTube 影片與音訊>`
2. **介面風格**：`<請填寫你想要的介面外觀，例如：極簡暗色系風格，有明顯的下載進度條>`
3. **特定功能**：`<請填寫特別需要的功能，例如：需要預覽播放器、保留下載歷史紀錄>`
